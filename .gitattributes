# 统一换行符：仓库内文本文件一律 LF，避免 Windows CRLF 反复告警
* text=auto eol=lf

# Windows 批处理必须 CRLF
*.bat text eol=crlf
*.cmd text eol=crlf

# 二进制文件不做任何转换
*.png binary
*.jpg binary
*.gif binary
*.ico binary
*.pdf binary
*.zip binary
